import { useTelegramBot } from '#wbm/bot-telegram'

/**
 * Точка инициализации глобальных подсистем.
 *
 * Этот скрипт выполняется первым и обеспечивает централизованный
 * запуск критических сервисов.
 *
 * ⚠️ Только инициализация. Никакой бизнес-логики.
 *
 * Если этот файл отсутствует, владелец глобального состояния
 * определится случайно — первым скриптом, который попытается к нему обратиться.
 *
 * Явное объявление устраняет неопределённость.
 *
 **/

useTelegramBot()
