{
  "scripts": {
    "test": "vitest run --config @mirta/testing/config"
  },
  "devDependencies": {
    "@mirta/testing": "0.4.12",
    "vitest": "^4.0.18",
    "vitest-mock-extended": "^3.1.0"
  }
}
